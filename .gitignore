build/
# Out-of-tree build dirs (build-verify/ etc.) used to check a change against a
# clean configure. Artefacts only -- and one of them holds a copied uhd.dll,
# which is GPLv3 and must never be committed to a source tree we publish.
build*/
out/
.vs/
.idea/
*.user
*.pdb
*.ilk
*.exe
*.obj
*.lib
CMakeUserPresets.json

# release staging + zips (published as GitHub release assets, not committed)
dist/
